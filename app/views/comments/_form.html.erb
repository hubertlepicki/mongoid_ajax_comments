<%= form_for @comment, remote: true, html: {method: :put} do |f| %>
  <p>
    <%= f.label :content %>
    <br/>
    <%= f.text_area :content %>
  </p>
  <p>
    <%= f.submit "Post Comment" %>
  </p>
<% end %>
