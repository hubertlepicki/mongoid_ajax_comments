$("#new_comment").remove();
$("#<%= params[:commentable_type] %>_<%= params[:commentable_id] %>").append(
'<%= escape_javascript(render partial: "form") %>'
);

$("#new_comment").ajaxComplete(function(){
  $("#attachment").MultiFile({});
});

$("#new_comment").ajaxForm({dataType: 'script'});
$('html,body').animate({ scrollTop: $("#new_comment").offset().top }, { duration: 'slow', easing: 'swing'});
